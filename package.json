{
  "name": "tau",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["packages/*"],
  "scripts": {
    "check": "oxlint packages && tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "dev": "bun run --cwd packages/app src/cli.ts"
  },
  "devDependencies": {
    "@ai-sdk/openai-compatible": "^3.0.22",
    "@types/bun": "1.3.13",
    "oxlint": "1.77.0",
    "typescript": "5.9.3",
    "vitest": "4.1.9"
  }
}
